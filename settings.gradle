rootProject.name = 'template'

include 'calendar', 'course-management', 'identity', 'restrictions-management', 'room-management'

for (project in rootProject.children) {
    project.projectDir = file("subprojects/${project.name}")
}

